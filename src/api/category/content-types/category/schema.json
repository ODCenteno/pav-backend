{
  "kind": "collectionType",
  "collectionName": "categories",
  "info": {
    "singularName": "category",
    "pluralName": "categories",
    "displayName": "Categoría",
    "description": "Categoría principal para agrupar lugares (experiencias, hospedaje, restaurantes, servicios, sitios)."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Nombre de la categoría. Ejemplo: Experiencias, Hospedaje, Restaurantes."
    },
    "slug": {
      "type": "uid",
      "targetField": "name",
      "required": true,
      "description": "Identificador único en la URL. COMPARTIDO entre idiomas: mismo slug para español e inglés."
    },
    "color": {
      "type": "string",
      "default": "#5A8A80",
      "description": "Color hexadecimal de la categoría. Ejemplo: #5A8A80"
    },
    "order": {
      "type": "integer",
      "default": 0,
      "description": "Orden de aparición. Los números menores aparecen primero."
    },
    "listings": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::listing.listing",
      "mappedBy": "category",
      "description": "Lugares que pertenecen a esta categoría."
    }
  }
}